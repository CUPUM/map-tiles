# Render from the full quebec data
# tilemaker 	--input input/quebec-latest.osm.pbf \
# 			--output output \
# 			--store store

# Render from the cropped data
tilemaker 	--input input/quebec-cropped-latest.osm.pbf \
			--output output \
			--store store